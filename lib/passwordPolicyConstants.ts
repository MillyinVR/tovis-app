// lib/passwordPolicyConstants.ts
//
// Kept separate from lib/passwordPolicy.ts so client components can show the
// minimum-length rule without bundling the 10k breached-passwords list.

export const PASSWORD_MIN_LEN = 10
