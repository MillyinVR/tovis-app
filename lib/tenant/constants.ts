// lib/tenant/constants.ts
//
// See docs/architecture/tenant-model.md for the tenant contract.

/** Reserved slug for the root tenant (the TOVIS marketplace itself). */
export const TOVIS_ROOT_TENANT_SLUG = 'tovis-root'

/** Display name used when creating the root tenant. */
export const TOVIS_ROOT_TENANT_NAME = 'TOVIS'
